import 'package:flutter/material.dart';

class HyperStudentHome extends StatelessWidget {
  const HyperStudentHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
